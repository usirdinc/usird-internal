import React from 'react';
import { Container, Typography } from '@mui/material';

const ReportViewing = () => {
  return (
    <Container>
      <Typography variant="h4">View Reports</Typography>
      {/* Report viewing logic */}
    </Container>
  );
};

export default ReportViewing;
